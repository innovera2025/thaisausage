# DO write เข้า ERP สำเร็จใบแรก

Date: 18-09-26  
Status: ✅ VERIFIED — เขียนจริง 2 ใบ อ่านกลับมาเทียบแล้วตรงทุกคอลัมน์ · callback ยังไม่เขียนอัตโนมัติ  
Environment: VPS `krs-cloud`, commit `47af28e`

## ผลการเขียนจริง

| | |
|---|---|
| เอกสาร | `DoNo` = `TEST-ERP-001` (ข้อมูลทดสอบ ไม่ใช่เอกสารทางบัญชี) |
| `TransactionNo` | 11504 — ระบบจัดสรรเองด้วย `MAX+1` |
| แถวที่เพิ่ม | `tbl_DOhdr` 11503 → 11504 · `tbl_Dodtl` 29358 → 29359 |
| ตรวจกลับ | `verify_do` เทียบ 20 คอลัมน์ที่มีค่า → ตรงทั้งหมด |

ลำดับที่ทำจริง: ซ้อมเขียน (`rollback_only`) → ยืนยันว่าไม่มีแถวเหลือ → ขออนุมัติ → `--commit` →
อ่านกลับมาเทียบ

## ใบที่สอง — หลังย้ายมาช่วงเลขที่กันไว้

| | |
|---|---|
| เอกสาร | `DoNo` = `TEST-ERP-002` |
| `TransactionNo` | **900000** — เลขแรกของช่วงที่ ERP กันให้ |
| แถวที่เพิ่ม | `tbl_DOhdr` 11542 → 11543 · `tbl_Dodtl` 29473 → 29474 |
| ตรวจกลับ | ตรงทุกคอลัมน์ รวม 6 คอลัมน์ที่เติมค่าตั้งต้นให้ |

ระหว่างใบแรกกับใบที่สอง แอป ERP คีย์เอกสารเพิ่ม 38 ใบ (11504 → 11542) ซึ่งเป็นเหตุผลที่การใช้
`MAX+1` ร่วมกับเขาไม่ปลอดภัย

## สิ่งที่พิสูจน์ได้

- credential ฝั่งเขียนแยกจากฝั่งอ่านจริงตามข้อกำหนด Phase 07 และใช้งานได้
- mapping ทั้ง 79 คอลัมน์ตรงกับ schema จริง — ถ้าผิดสักคอลัมน์ SQL Server จะปฏิเสธตั้งแต่ตอนซ้อม
- `EntryDate` มาจากนาฬิกา SQL Server (`11:14:00.533`) ไม่ใช่นาฬิกาเรา
- ภาษาไทยเข้าครบไม่เพี้ยน ทั้งชื่อลูกค้าและชื่อสินค้า
- ชนิดข้อมูลแปลงถูก: `2026-09-18` → `datetime`, `595` → `595.0000`, `10` → `10.00`

## TransactionNo — การชนกันที่เกิดขึ้นจริง และทางแก้

ทีม ERP ยืนยันว่าแอปของเขาออกเลขด้วย `SELECT MAX + 1` เหมือนกัน เราจึงใช้วิธีเดียวกันบวก
`WITH (UPDLOCK, HOLDLOCK)` — **แล้วมันชนกันจริงภายในไม่กี่ชั่วโมง**

ใบแรกของเราได้เลข 11504 และแอป ERP ก็ออกเลข 11504 ให้ `DO-2609-1878` ผลคือหัวเอกสารสองแถวใช้
เลขเดียวกัน ข้อมูลของเราถูกทับ และรายการสินค้าทดสอบของเราไปผูกกับเอกสารจริงของเขา ฐานข้อมูลไม่ฟ้อง
เพราะ PK คือ `(RowOrder, TransactionNo)` ที่ `RowOrder` เป็น identity ทำให้คีย์ไม่ซ้ำเสมอ

สาเหตุที่ `UPDLOCK` ไม่ช่วย: มันกันได้เฉพาะผู้เขียนที่ขอล็อกแบบเดียวกัน แต่แอป ERP อ่าน `MAX`
ด้วย `SELECT` ธรรมดาซึ่งไม่ติดล็อกนั้น

แก้สองชั้น (`84b4887`)

1. **ช่วงเลขแยก** — `transaction_no_minimum: 900000` ทำให้เรานับ `MAX` เฉพาะในช่วงของเราเอง
   ทีม ERP ยืนยันแล้วว่าช่วงนี้ว่างและแอปของเขาจะไม่วิ่งไปถึง
2. **ด่านก่อน commit** — หลัง INSERT แต่ก่อน commit ระบบถามว่าเลขนี้มีกี่เอกสาร ถ้าไม่ใช่ 1
   (คือแถวของเราแถวเดียว) จะยกเลิกทั้งชุด ไม่ commit

ทีม ERP ลบสองแถวที่ปนเข้าไปใน `DO-2609-1878` ให้แล้ว

## ค้างอยู่

| # | เรื่อง | เจ้าของ |
|---|---|---|
| 1 | ~~คอลัมน์ไหนที่ห้ามเป็น NULL~~ ✅ ตอบแล้ว 6 คอลัมน์ · `Company`/`Comname` ยังไม่ต้อง | ERP |
| 2 | ~~ลบใบทดสอบ 11504~~ ✅ ลบแล้ว · เหลือลบ `TransactionNo` 900000 หลังตรวจเสร็จ | ERP |
| 3 | บัญชี SQL ที่ `INSERT` ได้เฉพาะสองตาราง แทน `sa` | ERP/DBA |
| 4 | ใบรับรอง SSL ของ SQL Server เพื่อเลิกใช้ `TrustServerCertificate=yes` | ERP/DBA |
| 5 | master ลูกค้า `CT-LR-BKK-2088` — SO ค้าง 2 ใบ | eVRP |
| 6 | ส่งคู่มือ + API key แล้วให้ eVRP ยิง DO ทดสอบเข้ามาเอง | เรา/eVRP |
| 7 | เปิด `do_write.enabled=true` ให้ callback เขียนเอง — รอข้อ 1 ก่อน | เรา |
| 8 | เปลี่ยน `THAISAUSAGE_API_KEY`, `VRP_TOKEN`, `ERP_SQL_PASSWORD` หลังทดสอบจบ | เรา |

## เครื่องมือที่เพิ่มระหว่างทาง

- `python -m thaisausage.verify_do --transaction-no N [--receipt-id ID]` — อ่านเอกสารกลับจาก ERP
  แล้วเทียบกับที่ส่งไปทีละคอลัมน์ อ่านอย่างเดียว รันได้ตลอด
- `python -m thaisausage.status --days N` — สรุปว่าส่ง SO ไปเท่าไร ค้างอะไร DO เข้ามากี่ใบ
  อะไรต้องให้คนดู อ่านจากฐานเราอย่างเดียว
- `python3 deploy/assemble_config.py [--check]` — ประกอบ/ตรวจ config จากสองไฟล์ที่อยู่ใน git

## บั๊กที่เจอและแก้ระหว่างทาง

1. `--transaction-no` ยังบังคับใส่ ทั้งที่ระบบออกเลขเองได้ → ซ้อมเขียนตามคู่มือจะ error (`1f07df0`)
2. ตัวอย่าง JSON ในสัญญาบอก `item_code` แต่ mapping บอก `itemcode` → ใบที่ทำตามตัวอย่างถูกปฏิเสธ
   ถ้าไม่เจอตอนนี้ eVRP จะไปเจอเองตอนพัฒนา (`6da3442`)
3. callback ข้ามการเขียนทุกครั้งที่ไม่ได้รับ `TransactionNo` — ซึ่งคือทุกครั้ง เพราะ eVRP ไม่ต้องส่ง
   มา แปลว่าต่อให้เปิด `enabled` ก็จะไม่มีวันเขียน (`6da3442`)

## คำถามที่เปิดค้างไว้ (22-09-26)

### ค่าประจำบริษัทในหัว DO

`Company`, `Comname`, `VatType` แมปไว้แล้วแต่ยังไม่มีค่า เพราะ eVRP ไม่รู้จัก — SQL ของ SO ใช้
`Company` เป็นเงื่อนไขกรอง (`= 'TST'`) ไม่ได้ดึงออกมาส่ง ข้อมูลจาก ERP แสดงว่ามีอย่างน้อยสองบริษัท

| Company | Comname | VatType |
|---|---|---|
| `TST` | ห้างหุ้นส่วนจำกัด ไทยซอสเทรดดิ้ง | 1 |
| `NSF` | นิวสยามฟู้ด โดย นาย อุดร ยงศิริวรรณ | 3 |

ตอนนี้กวาดเฉพาะ `TST` จึงตั้งเป็นค่าคงที่ได้ แต่ถ้าวันหนึ่งเปิด `NSF` ด้วย ต้องเปลี่ยนเป็นค่าที่ผูกกับ
SO ต้นทาง ไม่ใช่ค่าคงที่ — เช่นเดียวกับ `LocationCode` / `LocationName` ที่ยังไม่มีคำตอบว่าเอามาจากไหน

### COD

ระบบส่ง `payment_in_day` ไปตรงๆ ไม่ได้คำนวณอะไร: `0` = COD, `> 0` = เครดิต, ว่าง = `null`
ที่ eVRP ไม่ถือเป็น COD ต้องยืนยันกับฝ่ายขายว่า "ช่องว่าง" ในทางธุรกิจหมายถึงอะไร ถ้าหมายถึงเงินสด
ตอนนี้เราส่งผิด แก้ได้ด้วยการเอา `NULLIF` ออกจาก SQL

### เส้นทางสำหรับแก้ DO ที่เขียนไปแล้ว — ทำแล้ว (22-09-26)

`POST /api/v1/vrp/do-updated` แยกจากเส้นสร้าง เพราะเส้นเดิมออกแบบมาให้ปฏิเสธการแก้โดยเจตนา
(`409` เมื่อเนื้อหาเปลี่ยน, unique index บน `do_no`) การเปลี่ยนพฤติกรรมเส้นเดิมจะทำให้เกราะกันข้อมูล
เพี้ยนหายไปด้วย

**คีย์ที่ส่งกลับให้ eVRP** — `transaction_no` อยู่ระดับบนสุดของคำตอบทั้งสองเส้นแล้ว เป็น `null`
จนกว่าจะเขียนสำเร็จ พร้อม `do_no` ที่สะท้อนกลับไป **ห้ามรับ `transaction_no` กลับมาทาง HTTP** —
ระบบค้นเองจาก `do_no` ผ่าน `written_document()` ถ้ายอมรับเลขจากภายนอก เท่ากับเปิดให้แก้เอกสารใบไหน
ใน ERP ก็ได้เพียงเดาเลข

การตัดสินใจที่เลือกไว้ (ยังไม่ได้ยืนยันกับทีม ERP ทั้งหมด)

| คำถาม | ที่ทำ | เหตุผล |
|---|---|---|
| แก้คอลัมน์ไหนได้ | ทุกคอลัมน์ที่แมปไว้ ยกเว้น `TransactionNo` และ `EntryDate` | เลขคือตัวชี้แถว ส่วน `EntryDate` คือเวลาที่สร้าง |
| รายการสินค้า | ลบทั้งหมดแล้วใส่ใหม่ | eVRP ส่งเอกสารฉบับที่เขาเชื่อ ไม่ได้ส่งรายการแก้ |
| ถ้า ERP แก้ไปแล้ว | เขียนทับ | ✅ ยืนยัน 22-09-26: ข้อมูลจาก eVRP ถือเป็นฉบับจริง |
| ห้ามแก้เมื่อไหร่ | `IsApproved` / `IsClosed` / `IsAcc` = 1 | เอกสารเข้ากระบวนการของ ERP แล้ว |
| การยกเลิก | ไม่ทำ และจะไม่ทำ | ✅ ยืนยัน 22-09-26: เป็นสิทธิ์ของทีม ERP เท่านั้น |

ผลที่ตามมาจากข้อ "เขียนทับ": ถ้าทีม ERP แก้เอกสารที่มาจาก eVRP ไว้ แล้ว eVRP ส่งฉบับแก้มาทีหลัง
การแก้ของทีม ERP จะหายไปเงียบๆ **ต้องแจ้งทีม ERP ให้ทราบ** ทางกันคือให้เขา Approve หรือ Close
เอกสารที่ไม่ต้องการให้ถูกแก้อีก ซึ่งระบบจะปฏิเสธการแก้ทันที

การลบรายการสินค้าเป็นคำสั่ง `DELETE` กับ ERP จริง จึงมี flag แยกของตัวเอง `do_write.update_enabled`
(ค่าเริ่มต้น `false`) ไม่พ่วงกับ `enabled` ที่ใช้ตอนสร้าง และทุกอย่างอยู่ในทรานแซกชันเดียวกับการแก้หัว
เอกสาร ถ้าล้มเหลวตรงไหนเอกสารคงเดิมทั้งใบ

อยู่ในเอกสารตรงไหน

| เรื่อง | ไฟล์ | หัวข้อ |
|---|---|---|
| คีย์ที่ส่งกลับ `transaction_no` | `docs/eVRP_DO_Callback_Spec.md` | §4 และ §4.1 |
| เส้นทางแก้ไข DO | `docs/eVRP_DO_Callback_Spec.md` | §10, §10.1 สิ่งที่ต้องส่ง, §10.2 คำตอบ, §10.3 กติกา |
| สัญญา API | `docs/openapi.json` | `/api/v1/vrp/do-updated` และคำอธิบาย `202` ของ `/api/v1/vrp/do-received` |
| โค้ด | `thaisausage/do_writer.py` | `build_update()`, `assert_can_be_updated()`, `DOWriter.update()` |
| โค้ด | `thaisausage/service.py` | `written_document()`, `update_do()`, `attempt_do_update()`, ตาราง `do_updates` |
| เทส | `tests/test_do_update.py` | 17 เทส ครอบคลุมการปฏิเสธทุกแบบ |
